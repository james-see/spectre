# Deprecated RuView UI mounts

These patched RuView files are **no longer the Spectre product UI**.

Use [`../web/`](../web/) (Astro + Svelte) instead:

```bash
cd ~/p/spectre/web && npm run dev
```

This directory may be removed once you no longer need the old Observatory patches.
